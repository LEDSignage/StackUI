# Copy to .env and edit. Untracked, so machine-specific addresses stay local.

# Where ComfyUI is. Leave unset when Stack UI runs on the ComfyUI machine.
# Developing from another workstation, point it at the box:
# COMFY_URL=http://10.0.0.1:8188

# What Stack UI listens on.
# PORT=8790
